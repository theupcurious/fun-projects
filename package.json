{
  "name": "fun-projects",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:gradient": "npm run dev --prefix gradient",
    "dev:og-previewer": "npm run dev --prefix og-previewer",
    "dev:vibe": "npm run dev --prefix vibe"
  }
}
